build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py migrate
    - python manage.py collectstatic

run:
  internetspeedtester:
    command:
      - "cd app && python -m apps.internet_speedtester.tasks"
    image: web
#  worker:
#    command:
#      - celery --app=my_app beat -l info
#    image: web